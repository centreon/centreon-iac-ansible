---
  - name: reload systemctl
    shell: systemctl daemon-reload
    listen: "reload systemctl daemons"
    
  - name: restart httpd
    service: name=httpd24-httpd state=restarted
    listen: "restart httpd24-httpd"
